## Integrations scaffold

Auto-generated folder structure organized by `integration_domains` → `tools`.

- Each domain folder contains tool subfolders.
- Each tool folder contains placeholders for auth/client/evidence collection modules.

Regenerate via:

```powershell
python scripts/generate_integration_scaffold.py
```
